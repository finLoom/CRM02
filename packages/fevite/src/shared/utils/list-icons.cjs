const Icons = require('@fluentui/react-icons');
import { Add24Regular } from "@fluentui/react-icons";
import { Add24Regular } from "@fluentui/react-icons";

console.log(Object.keys(Icons).filter(name => name.toLowerCase().includes('collab')));
